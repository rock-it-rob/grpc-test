syntax = "proto3";

package rob.proto.grpc.api;

message EchoRequest {
    string content = 1;
}

message EchoResponse {
    string content = 1;
}

service EchoService {
    rpc Echo (EchoRequest) returns (EchoResponse);
}